# Shared secret for the on-demand cache revalidation webhook
# (src/app/api/revalidate/route.ts). Must match POPSERIES_REVALIDATE_SECRET
# configured in the WordPress plugin (Pop Series admin, or wp-config.php).
# Generate one with:  openssl rand -hex 32
REVALIDATE_SECRET=
